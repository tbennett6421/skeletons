filter {

    ## General
    mutate { 
        id => "2002_normalize_type-conversion.conf :: filter//mutate-convert(bools)"
        convert => {
            "[user][admin]" => "boolean"
        }
    }

} ## End Filter
